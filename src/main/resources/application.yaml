camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
  filter:
    create: All tasks
  license-file: classpath:license.key.txt
  history-level: full
  deployment-resource-pattern: classpath:/processesc7/**/*.bpmn

camunda:
  client:
    mode: self-managed
    zeebe:
      enabled: true
      grpc-address: http://127.0.0.1:26500
      rest-address: http://127.0.0.1:8080

server.port: 8080